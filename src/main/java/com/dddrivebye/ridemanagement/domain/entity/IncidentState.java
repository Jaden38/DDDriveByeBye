package com.dddrivebye.ridemanagement.domain.entity;

public class IncidentState implements RideState {
    // Handle incident (e.g., might transition back to InProgress or Cancelled after manual intervention)
}
